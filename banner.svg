<svg width="650" height="90" viewBox="0 0 650 90" fill="none" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <style>
      /* Default / Dark Mode */
      .banner-bg { fill: #0d1117; }
      .banner-dot { fill: #238636; opacity: 0.28; }
      .quote-green { fill: #39d353; }
      .quote-accent { fill: #ffffff; font-weight: 700; }

      /* GitHub Light Mode Overrides */
      @media (prefers-color-scheme: light) {
        .banner-bg { fill: #f6f8fa; }
        .banner-dot { fill: #1a7f37; opacity: 0.20; }
        .quote-green { fill: #1a7f37; }
        .quote-accent { fill: #1f2328; font-weight: 700; }
      }
    </style>

    <!-- Dynamic Dot Pattern -->
    <pattern id="minimalDots" x="0" y="0" width="16" height="16" patternUnits="userSpaceOnUse">
      <circle cx="2" cy="2" r="1.1" class="banner-dot" />
    </pattern>
  </defs>

  <!-- Adaptive Background -->
  <rect width="650" height="90" rx="8" class="banner-bg" />

  <!-- Dotted Texture Accents -->
  <rect x="12" y="10" width="55" height="70" rx="4" fill="url(#minimalDots)" />
  <rect x="583" y="10" width="55" height="70" rx="4" fill="url(#minimalDots)" />

  <!-- Line 1 -->
  <text x="325" y="38" text-anchor="middle" font-family="-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif" font-size="11" font-weight="500" letter-spacing="0.3">
    <tspan class="quote-green">To have </tspan>
    <tspan class="quote-accent">something</tspan>
    <tspan class="quote-green"> you’ve never had before,</tspan>
  </text>

  <!-- Line 2 -->
  <text x="325" y="65" text-anchor="middle" font-family="-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif" font-size="11" font-weight="500" letter-spacing="0.3">
    <tspan class="quote-green">you must become </tspan>
    <tspan class="quote-accent">someone</tspan>
    <tspan class="quote-green"> you’ve never been before.</tspan>
  </text>
</svg>
